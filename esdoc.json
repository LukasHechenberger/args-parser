{
  "source": "./src",
  "destination": "./docs/api",
  "access": ["public", "package", "private"],
  "test": {
    "type": "mocha",
    "source": "./test"
  },
  "plugins": [
    { "name": "esdoc-plugin-require-coverage" }
  ]
}
